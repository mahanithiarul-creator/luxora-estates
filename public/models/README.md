Place high-fidelity GLTF/GLB models here for the 3D scenes.

Recommended files:
- city.glb — futuristic skyscraper city scene
- villa.glb — luxury villa model
- penthouse.glb — penthouse model

Use `useGLTF('/models/your-model.glb')` in components/Scene/AdvancedCanvasScene.tsx or add new loaders.
