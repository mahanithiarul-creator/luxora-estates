Place DRACO decoder files here to enable DRACO-compressed GLTFs.

Recommended files:
- draco_decoder.wasm
- draco_decoder.js
- draco_wasm_wrapper.js

When using the loader config in `utils/three/loaders.ts`, the DRACO decoder path is set to `/draco/`.
